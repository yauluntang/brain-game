/*
import 'phaser';

export class GridObject extends Phaser.GameObjects.Container {
  grid?: Phaser.GameObjects.Rectangle;
  constructor(scene: Phaser.Scene, x: number, y: number) {
    super(scene, x, y);
    this.scene = scene;
    this.x = x;
    this.y = y;
    this.grid = this.scene.add.rectangle(32, 32, 64, 64);
    this.grid.setStrokeStyle(2, 0x1a65ac);
    this.add(this.grid);
    this.scene.add.existing(this);
  }

}*/